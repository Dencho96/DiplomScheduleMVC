@model IEnumerable<DiplomScheduleMVC.Models.Auditory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AuditoryNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AuditoryNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AuditoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AuditoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AuditoryID })
        </td>
    </tr>
}

</table>
