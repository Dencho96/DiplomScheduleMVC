@model IEnumerable<DiplomScheduleMVC.Models.Teacher>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Додати", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TeacherName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TeacherName)
        </td>
        <td>
            @Html.ActionLink("Редагувати", "Edit", new { id=item.TeacherID }) |
            @Html.ActionLink("Деталі", "Details", new { id=item.TeacherID }) |
            @Html.ActionLink("Видалити", "Delete", new { id=item.TeacherID })
        </td>
    </tr>
}

</table>
